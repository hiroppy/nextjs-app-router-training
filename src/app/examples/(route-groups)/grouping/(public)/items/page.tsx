import { Boundary } from "@/app/_components/boundary";

export default function Page() {
  return (
    <Boundary label="Public Page">
      <p>file path: grouping/(public)/items</p>
    </Boundary>
  );
}
